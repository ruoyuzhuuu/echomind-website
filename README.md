# 回声思维 EchoMind - 播客网站

一个现代化的播客网站，采用深色科技感设计，支持首页展示和单集详情页面。

## 🚀 技术栈

- **框架**: Next.js 16 (App Router)
- **UI**: React 19 + TypeScript
- **样式**: Tailwind CSS 4
- **动画**: Framer Motion
- **字体**: Inter (英文) + Noto Sans SC (中文)

## 🎨 设计特点

### 视觉设计
- 深色主题 (#0A0A0A 背景)
- 紫青渐变色 (#6C63FF → #00C2FF)
- 发光效果和轻浮动画
- 响应式布局 (移动端/平板/桌面)

### 页面结构

#### 首页 (`/`)
- Header (固定顶部导航)
- Hero Banner (最新一期展示)
- Highlights (3个精彩看点卡片)
- Recent Episodes (6个最近节目栅格)
- Footer

#### 单集页面 (`/episodes/[id]`)
- Episode Header (标题、日期、标签)
- Audio Player (播放/暂停、10s跳转、倍速、章节刻度)
- Transcript Panel (中英字幕切换、点击跳转)
- Chapters Navigation (章节目录)
- Guest Card (嘉宾信息)
- Summary Block (节目摘要)
- Key Points (精彩要点)
- Highlights (高光片段)
- Resources (延伸阅读)

## 📦 项目结构

```
echomind-website/
├── app/
│   ├── layout.tsx          # 根布局
│   ├── page.tsx            # 首页
│   ├── globals.css         # 全局样式
│   └── episodes/[id]/
│       └── page.tsx        # 单集页面
├── components/
│   ├── Header.tsx          # 顶部导航
│   ├── Footer.tsx          # 底部信息
│   ├── Logo.tsx            # 品牌Logo
│   ├── Button.tsx          # 按钮组件
│   ├── GradientRing.tsx    # 背景装饰
│   ├── HeroCard.tsx        # 首页Hero卡片
│   ├── HighlightCard.tsx   # 亮点卡片
│   ├── EpisodeCard.tsx     # 节目卡片
│   ├── EpisodeHeader.tsx   # 单集头部
│   ├── AudioPlayer.tsx     # 音频播放器
│   ├── TranscriptPanel.tsx # 字幕面板
│   ├── ChaptersNav.tsx     # 章节导航
│   ├── GuestCard.tsx       # 嘉宾卡片
│   ├── SummaryBlock.tsx    # 摘要块
│   ├── KeyPointCard.tsx    # 要点卡片
│   └── ResourceItem.tsx    # 资源链接
├── data/
│   ├── home.json           # 首页数据
│   └── episodes/
│       └── huberman-immune-system.json  # 单集数据示例
├── public/
│   ├── images/             # 图片资源
│   └── audio/              # 音频文件
├── tailwind.config.ts      # Tailwind配置
├── next.config.js          # Next.js配置
└── package.json
```

## 🛠️ 开发指南

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000 查看网站

### 构建生产版本

```bash
npm run build
npm start
```

## 📝 数据模型

### 首页数据 (`data/home.json`)

```json
{
  "brand": {
    "nameCn": "回声思维",
    "nameEn": "EchoMind",
    "tagline": "在 AI 与声音的边界，捕捉思想的回声"
  },
  "latest": {
    "id": "episode-id",
    "cover": "/images/cover.jpg",
    "titleCn": "中文标题",
    "titleEn": "English Title",
    "summary": "简短摘要",
    "audioUrl": "/audio/episode.mp3"
  },
  "highlights": [...],
  "episodes": [...]
}
```

### 单集数据 (`data/episodes/[id].json`)

```json
{
  "id": "episode-id",
  "titleCn": "中文标题",
  "titleEn": "English Title",
  "date": "2025-11-10",
  "duration": 4929,
  "tags": ["标签1", "标签2"],
  "cover": "/images/cover.jpg",
  "audioUrl": "/audio/episode.mp3",
  "chapters": [...],
  "captions": [...],
  "guest": {...},
  "summary": {...},
  "keyPoints": [...],
  "highlights": [...],
  "resources": [...]
}
```

## 🎯 功能特性

### 音频播放器
- ✅ 播放/暂停控制
- ✅ 10秒快进/后退
- ✅ 多倍速播放 (0.8x - 2x)
- ✅ 进度条章节标记
- ✅ 时间显示

### 字幕系统
- ✅ 中英文切换
- ✅ 双语对照模式
- ✅ 当前句高亮
- ✅ 点击跳转播放
- ✅ 自动滚动

### 响应式设计
- ✅ 移动端优化 (<768px)
- ✅ 平板适配 (768-1279px)
- ✅ 桌面端 (≥1280px)
- ✅ 触控友好

### 可访问性 (a11y)
- ✅ 键盘导航
- ✅ ARIA 标签
- ✅ 焦点可见
- ✅ 颜色对比度 ≥ 4.5:1

## 🎨 设计 Tokens

```css
--bg: #0A0A0A          /* 背景色 */
--card: #1A1A1A        /* 卡片色 */
--text: #EAEAEA        /* 文字色 */
--muted: #A6A6A6       /* 次要文字 */
--accent-from: #6C63FF /* 渐变起点（紫）*/
--accent-to: #00C2FF   /* 渐变终点（青）*/
--border: rgba(255,255,255,0.08)  /* 边框 */
--glow: 0 0 16px rgba(108,99,255,.35)  /* 发光效果 */
```

## 📖 使用说明

### 添加新节目

1. 在 `data/episodes/` 创建新的 JSON 文件
2. 按照数据模型填写节目信息
3. 将封面图片放到 `public/images/`
4. 将音频文件放到 `public/audio/`
5. 更新 `data/home.json` 添加到节目列表

### 自定义样式

修改 `tailwind.config.ts` 和 `app/globals.css` 中的设计 tokens

## 🚧 待实现功能

- [ ] 搜索功能
- [ ] 评论系统
- [ ] 分享功能
- [ ] WebVTT 字幕文件支持
- [ ] RSS 订阅
- [ ] 播放列表
- [ ] 用户收藏

## 📄 License

MIT

## 👥 贡献

欢迎提交 Issue 和 Pull Request！
